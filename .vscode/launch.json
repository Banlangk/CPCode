{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run and Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build"
        }
    ]
}
